#ifndef __BOARD_H__
#define __BOARD_H__


#include "Constants.h"
#include "Piece.h"
#include "Move.h"
#include "Move_Log.h"


/*
Board init : log and captured pieces init also (just have to call the init of the log and the captured pieces, that are in other files)


changing the board

make move : modifed also the log and the captured pieces 
--> make_move pour le board, le log, les captured piece

undo move : modifed also the log and the captured pieces 
--> undo_move pour le board, le log, les captured piece


clear board : close everything and free the memory 
*/




#endif /* __BOARD_H__ */